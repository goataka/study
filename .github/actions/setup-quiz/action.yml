# app ディレクトリの依存関係をセットアップするコンポジットアクション
# 各ジョブで繰り返される「npm ci」を共通化する
name: "Setup App Dependencies"
description: "app の依存関係をインストールする"

runs:
  using: "composite"
  steps:
    - name: 依存関係のインストール
      shell: bash
      working-directory: ${{ github.workspace }}/app
      run: npm ci
